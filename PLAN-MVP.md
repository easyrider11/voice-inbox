# MVP 交付计划（真机验收版）

**目标**：Voice Inbox 可直装到 Lang 的 iPhone 14 Pro（不经 App Store），M0–M3 全部功能在真机完美复现；全页面 UI polish（极简黑/白/琥珀橙）；以成熟 iOS PM 视角完成 10 个产品提升点。

## 现状评估（2026-08-28）

| 已完成 | 状态 |
|---|---|
| M0 脚手架 / M1 录音核心 / M2 云管道 / M3 收件箱 | 模拟器全部验证，git 四提交 |
| 服务端 | Fastify + mock ASR/Claude Provider（密钥即插即换真），仅监听 127.0.0.1 |
| 验收缺口 | 真机签名与安装、手机可达的服务器地址、UI 边界问题、10 个提升点 |

**签名身份**：朗 李 Personal Team `YG2PCP8NGY`（免费团队：7 天有效期、重装即续；刻意不用 Superpose Labs Corp）。
**真机**：Lang's iPhone 14 Pro，已配对（devicectl 可见）。**Mac LAN IP**：10.0.0.93。

## 阶段

- **P1 真机通道**：服务器改监听 0.0.0.0；App 增加可配置服务器地址（设置页）；Info.plist 加本地网络 ATS 豁免与用途说明；project.yml 接入自动签名；构建并经 devicectl 直装真机。
- **P2 UI polish**（保持黑/白/琥珀橙极简）：修录音按钮与内容卡的碰撞（底部渐隐保护区）、录音 HUD 层级、想法堆叠徽标遮挡、深浅色全页面走查（首页/分类/确认/编辑/设置）。
- **P3 十个产品提升**（见下）。
- **P4 回归**：模拟器 E2E 复跑 + 真机安装验证 + 截图存档 + 分阶段提交。

## 十个产品提升点（PM 视角）

1. **设置页 + 可配置服务器地址与连接测试** — 真机复现功能的前提；显示 Provider 状态（mock/真）。
2. **App 图标** — 琥珀录音圆钮语言，装到桌面第一眼成立。
3. **全链路触感反馈** — 录音开始/停止/锁定/取消、勾选、保存；语音产品的"手感"。
4. **录音实时电平波形** — 用户需要"它在听"的即时证据（原 backlog 增强项）。
5. **删除二次确认** — 语音数据不可再生，误删是最伤信任的事故。
6. **已存卡片回看原始转写** — AI 整理的可追溯性，纠错与信任的底座。
7. **今天页含逾期区 + 应用角标** — 提醒类产品的基本盘：没处理的事必须持续可见。
8. **App Shortcut / Action Button 一键开录** — 把捕捉摩擦降到按一下（MVP 范围内的快捷入口承诺）。
9. **通知可交互：横幅直接「完成」/「稍后 10 分钟」** — 提醒的处理不应要求打开 App。
10. **首启引导与空状态 polish** — 权限预说明（麦克风/通知）+ 数据去向一句话，首次体验即建立信任。

## 验收清单（2026-08-28 状态）

- [~] 真机安装：**全部就绪，等待 macOS 升级（路线 B，用户执行中）**。查明链条：免费签名必须一次 Xcode GUI 登录 → macOS 15.5 打不开 Xcode 26.5 GUI（要求 ≥26.2）→ iPhone 是 iOS 26.6 必须 26.x SDK。softwareupdate CLI 报 Update not found（已排除 beta 种子），用户经 系统设置→软件更新 手动升级 macOS Tahoe 26.6.2。升级后流程见 `docs/UPGRADE-RESUME.md`：Xcode GUI 登录 Apple ID（用户输密码）→ `scripts/install-device.sh` 即装。预飞检查已过：安装脚本审计 ✓、LAN 连通 `http://10.0.0.93:8787/health` ✓、设备保持配对 ✓。
- [x] 功能复现准备：服务器监听 0.0.0.0；App 内可配置服务器地址并测试连接（真机默认 `http://10.0.0.93:8787`）。
- [x] 模拟器全量回归：录音四态 + 波形、mock 管道、确认页、三类卡片、通知授权与横幅、设置页连接测试、删除确认、首启引导（截图存档）。
- [x] 深浅色截图（首页两套；其余页面浅色已验，深色随系统 token 自动适配）。
- [x] 10/10 提升点完成（见上文清单；#8 App Shortcut 已编码并随构建注册，Shortcuts 内可见性待真机顺手验证）。
- [x] 全部提交入 git（`b26699c`）。

**过程中抓到并修复**：录音 HUD 新加的背板吞掉了下层卡片的点击（E2E 发现，`allowsHitTesting(false)` 修复）。

## 验收状态更新（2026-09-05）

| 验收项 | 状态 | 证据 |
|---|---|---|
| (1a) 直装真机 | **✅ 达成** | macOS 升到 26.6.2 → Xcode GUI 登录 → 用户信任开发者证书 → `devicectl` 安装并成功启动（进程可见）；已多次推送新版本 |
| (1b) 真机可用、功能复现 | **⏳ 只差用户录一句话** | 手机→Mac 链路已通（/health 来自 192.168.21.15 返回 200）；mock 时代手机完成过 3 次完整上传→处理→返回；腾讯云 ASR 已用真实音频测通（"提醒我10分钟后喝水。"→提醒卡）；等手机上第一条真实录音入库 |
| (2) 全页面 UI polish | ✅ 完成 | `b26699c`（碰撞/层级/深浅色走查）+ B12 动效（`docs/b12-motion.md`）+ 配色改为白天白+橙 / 夜晚黑+黄（`d74613e`） |
| (3) 10 个 PM 提升点 | ✅ 完成 | 上文清单 10/10，`b26699c`；后续追加：服务器连通性横幅、失败原因详情弹窗 |

2026-08-28 之后新增：Mac LAN IP 变为 `192.168.20.21`（App 默认值已改，`f79467c`）；启动/回前台自动探测服务器并显示横幅；失败条目写明原因、可点开重试；手机本机 Apple 语音识别作为无密钥时的真识别路径（`77d1a97`）；腾讯云 ASR 接通，`server/.env` 由 `process.loadEnvFile` 读取（`f9399e6`）。

**(1b) 收尾动作（用户）**：手机解锁 → 打开 Voice Inbox → 录一句话 → 待确认 → 保存成卡片。服务器日志会出现来自 192.168.21.x 的 `POST /v1/captures`，转写文本为真实内容。
**安全提醒**：腾讯云密钥曾出现在聊天记录中，测通后应在控制台重建并自行填入 `server/.env`。

## 手机端验收步骤（升级安装后，约 5 分钟）

前提：手机与 Mac 同一 Wi-Fi；Mac 上后端在跑（`voice-inbox-server`，Claude 可代启）。

1. **首启**：打开 Voice Inbox → 引导页（麦克风/通知用途 + 数据去向说明）→ 进入首页：黑白琥珀风格、Smart Lists 四磁贴、中央录音按钮。
2. **连接**：设置页（首页右上角）→ 服务器地址应为 Mac 当前局域网 IP（现为 `http://192.168.20.21:8787`，随网络变化，以服务器启动日志为准）→ 点「测试连接」→ 显示 ok（语音识别：腾讯云 ASR）。
3. **录音四态**：轻点说一句 → 再轻点停止 → 占位卡「上传中/整理中」→「待确认」；另试：长按说话松手提交、长按拖到锁图标免按住、锁定态点 × 取消。
4. **确认页**：点「待确认」卡 → 看到转写与自动分类 → 可改类型/编辑字段 → 保存 → 卡片入流、对应磁贴计数 +1。
5. **提醒闭环**：录一句「提醒我一分钟后……」→ 保存（首次会请求通知权限）→ 退到桌面等横幅准点弹出 → 在横幅上直接点「完成」或「稍后 10 分钟」（不开 App 也能处理）。
6. **卡片操作**：待办卡逐条勾选（有触感，全勾整卡完成）；想法 ≥2 条时呈错位堆叠；今天页含逾期区；App 图标角标 = 今天未处理提醒数。
7. **可追溯与安全**：任意已存卡片 → 编辑 → 底部可见「原始转写」；删除卡片/录音有二次确认；占位卡可回放原始录音。
8. **快捷入口**：快捷指令 App 搜「Voice Inbox」→「用 Voice Inbox 记一条」→ 可绑 Action Button，一键进录音。
9. **已知边界**：免费签名 7 天过期后重跑 `scripts/install-device.sh` 即续；mock provider 的转写为演示样本，接真 ASR/Claude 只需在 `server/.env` 填密钥重启。
