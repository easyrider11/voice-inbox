{
  "name": "voice-inbox-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin backend for Voice Inbox: auth, upload URLs, AI orchestration, quotas (PROJECT.md §7.3)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "fastify": "^5.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
